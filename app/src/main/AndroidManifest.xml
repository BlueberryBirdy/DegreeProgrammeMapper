<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.icecube.degreeprogrammemapper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ViewStudentModulesActivity"></activity>
        <activity android:name=".EditAboutActivity" />
        <activity android:name=".SelectPathwayActivity" />
        <activity android:name=".EditStudentActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".EditPathwayModulesActivity" />
        <activity
            android:name=".ViewStudentsActivity"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>